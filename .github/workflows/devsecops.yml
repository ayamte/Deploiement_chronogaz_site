name: DevSecOps Security Analysis  
  
on:  
  push:  
    branches: [main, develop, devsecops/vulnerable-testing]  
  pull_request:  
    branches: [main]  
  workflow_dispatch:  
  
jobs:  
  security-scan:  
    runs-on: ubuntu-latest  
    name: Static Security Analysis (SAST & SCA)  
  
    steps:  
      - name: Checkout code  
        uses: actions/checkout@v4  
  
      - name: Setup Node.js  
        uses: actions/setup-node@v4  
        with:  
          node-version: '18'  
          cache: 'npm'  
          cache-dependency-path: |  
            client/package-lock.json  
            api/package-lock.json  
  
      - name: Install validation tools  
        run: |  
          sudo apt-get update  
          sudo apt-get install -y jq tree  
  
      - name: Install Dependencies - Client  
        working-directory: ./client  
        run: |  
          echo "📦 Installation des dépendances Client..."  
          npm ci --legacy-peer-deps  
        continue-on-error: false  
  
      - name: Install Dependencies - API  
        working-directory: ./api  
        run: |  
          echo "📦 Installation des dépendances API..."  
          npm ci --legacy-peer-deps  
        continue-on-error: false  
            
      # ===================================  
      # SCA - Analyse des Dépendances  
      # ===================================  
  
      - name: SCA - NPM Audit (Client - Production)  
        working-directory: ./client  
        run: |  
          echo "🔍 SCA Client Production..."  
          npm audit --json > ../sca-client-production.json || true  
          npm audit --audit-level=moderate || true  
        continue-on-error: true  
  
      - name: SCA - NPM Audit (Client - Vulnerable)  
        working-directory: ./client  
        run: |  
          echo "🔍 SCA Client Vulnerable Mode..."  
          if [ -f "package.vulnerable.json" ]; then  
            cp package.json package.json.backup  
            cp package.vulnerable.json package.json  
            npm install --legacy-peer-deps  
            npm audit --json > ../sca-client-vulnerable.json || true  
            mv package.json.backup package.json  
          else  
            echo "⚠️ package.vulnerable.json non trouvé, scan ignoré"  
            echo '{"metadata":{"vulnerabilities":{"total":0}}}' > ../sca-client-vulnerable.json  
          fi  
        continue-on-error: true  
  
      - name: SCA - NPM Audit (API - Production)  
        working-directory: ./api  
        run: |  
          echo "🔍 SCA API Production..."  
          npm audit --json > ../sca-api-production.json || true  
          npm audit --audit-level=moderate || true  
        continue-on-error: true  
  
      - name: SCA - NPM Audit (API - Vulnerable)  
        working-directory: ./api  
        run: |  
          echo "🔍 SCA API Vulnerable Mode..."  
          if [ -f "package.vulnerable.json" ]; then  
            cp package.json package.json.backup  
            cp package.vulnerable.json package.json  
            npm install --legacy-peer-deps  
            npm audit --json > ../sca-api-vulnerable.json || true  
            mv package.json.backup package.json  
          else  
            echo "⚠️ package.vulnerable.json non trouvé, scan ignoré"  
            echo '{"metadata":{"vulnerabilities":{"total":0}}}' > ../sca-api-vulnerable.json  
          fi  
        continue-on-error: true  
  
      - name: SCA - OWASP Dependency Check  
        uses: dependency-check/Dependency-Check_Action@main  
        timeout-minutes: 30  
        with:  
          project: 'ChronoGaz'  
          path: '.'  
          format: 'JSON,HTML'  
          out: 'dependency-check-reports'  
        continue-on-error: true  
  
      - name: Upload SCA Reports  
        uses: actions/upload-artifact@v4  
        if: always()  
        with:  
          name: sca-reports  
          path: |  
            sca-client-production.json  
            sca-client-vulnerable.json  
            sca-api-production.json  
            sca-api-vulnerable.json  
            dependency-check-reports/  
          retention-days: 30  
          if-no-files-found: warn  
            
      # ===================================  
      # SAST - Analyse Statique du Code  
      # ===================================  
  
      - name: SAST - Diagnostic Structure Projet  
        run: |  
          echo "🔍 DIAGNOSTIC COMPLET DE LA STRUCTURE"  
          echo "========================================"  
          echo ""  
          echo "📍 Répertoire actuel: $(pwd)"  
          echo ""  
              
          echo "📂 Structure complète du projet:"  
          tree -L 3 -I 'node_modules|.git' || find . -maxdepth 3 -type d -not -path '*/node_modules/*' -not -path '*/.git/*' | head -50  
          echo ""  
              
          echo "🔍 Recherche EXHAUSTIVE de fichiers JS/JSX:"  
          echo "----------------------------------------"  
              
          if [ -d "client" ]; then  
            echo "✓ Dossier client/ existe"  
            echo ""  
            echo "  Fichiers .js dans client/:"  
            find client -type f -name "*.js" ! -path "*/node_modules/*" ! -path "*/build/*" | head -20  
            echo ""  
            echo "  Fichiers .jsx dans client/:"  
            find client -type f -name "*.jsx" ! -path "*/node_modules/*" ! -path "*/build/*" | head -20  
            echo ""  
            echo "  Total fichiers JS: $(find client -type f -name "*.js" ! -path "*/node_modules/*" ! -path "*/build/*" | wc -l)"  
            echo "  Total fichiers JSX: $(find client -type f -name "*.jsx" ! -path "*/node_modules/*" ! -path "*/build/*" | wc -l)"  
          else  
            echo "✗ Dossier client/ INTROUVABLE"  
          fi  
              
          echo ""  
              
          if [ -d "api" ]; then  
            echo "✓ Dossier api/ existe"  
            echo ""  
            echo "  Fichiers .js dans api/:"  
            find api -type f -name "*.js" ! -path "*/node_modules/*" | head -20  
            echo ""  
            echo "  Total fichiers JS: $(find api -type f -name "*.js" ! -path "*/node_modules/*" | wc -l)"  
          else  
            echo "✗ Dossier api/ INTROUVABLE"  
          fi  
              
          echo ""  
          echo "========================================"  
        continue-on-error: true  
  
      - name: SAST - ESLint Security Plugin  
        run: |  
          echo "🔧 Configuration SAST ESLint..."  
            
          # Installation GLOBALE  
          npm install -g eslint@8.57.0 eslint-plugin-security eslint-plugin-no-secrets  
            
          # Configuration ESLint - ✅ HERE-DOCUMENT CORRECTEMENT FERMÉ  
          cat > .eslintrc.security.json << 'EOF'  
          {  
            "env": {  
              "browser": true,  
              "es2021": true,  
              "node": true  
            },  
            "parserOptions": {  
              "ecmaVersion": 2021,  
              "sourceType": "module",  
              "ecmaFeatures": {  
                "jsx": true  
              }  
            },  
            "plugins": ["security", "no-secrets"],  
            "extends": ["plugin:security/recommended"],  
            "rules": {  
              "no-secrets/no-secrets": "warn",  
              "security/detect-eval-with-expression": "error",  
              "security/detect-non-literal-regexp": "warn",  
              "security/detect-unsafe-regex": "error",  
              "security/detect-buffer-noassert": "error",  
              "security/detect-child-process": "warn",  
              "security/detect-disable-mustache-escape": "error",  
              "security/detect-no-csrf-before-method-override": "error",  
              "security/detect-non-literal-fs-filename": "warn",  
              "security/detect-object-injection": "warn",  
              "security/detect-possible-timing-attacks": "warn",  
              "security/detect-pseudoRandomBytes": "error"  
            }  
          }  
          EOF  
            
          # Vérifier que le fichier a été créé  
          if [ ! -f ".eslintrc.security.json" ]; then  
            echo "❌ ERREUR: .eslintrc.security.json n'a pas été créé"  
            exit 1  
          fi  
            
          echo "✅ Configuration ESLint créée avec succès"  
            
          # Créer fichiers vides  
          echo '[]' > sast-eslint-client.json  
          echo '[]' > sast-eslint-api.json  
            
          # Scanner Client  
          if [ -d "client" ]; then  
            echo ""  
            echo "🔍 Scan SAST Client..."  
              
            CLIENT_JS_COUNT=$(find client -type f -name "*.js" ! -path "*/node_modules/*" ! -path "*/build/*" | wc -l)  
            CLIENT_JSX_COUNT=$(find client -type f -name "*.jsx" ! -path "*/node_modules/*" ! -path "*/build/*" | wc -l)  
              
            echo "  Fichiers à scanner: $CLIENT_JS_COUNT .js + $CLIENT_JSX_COUNT .jsx"  
              
            if [ $CLIENT_JS_COUNT -gt 0 ] || [ $CLIENT_JSX_COUNT -gt 0 ]; then  
              # Scan avec liste de fichiers  
              find client/src -type f \( -name "*.js" -o -name "*.jsx" \) ! -path "*/node_modules/*" ! -path "*/build/*" > /tmp/client-files.txt  
                
              if [ -s /tmp/client-files.txt ]; then  
                echo "  Fichiers trouvés:"  
                head -5 /tmp/client-files.txt  
                  
                cat /tmp/client-files.txt | xargs eslint \  
                  --config .eslintrc.security.json \  
                  --format json \  
                  --output-file sast-eslint-client.json 2>&1 || echo "  ⚠️ Scan terminé avec avertissements"  
              fi  
                
              echo "  ✅ Scan Client terminé"  
            else  
              echo "  ⚠️ Aucun fichier JS/JSX trouvé"  
            fi  
          fi  
            
          # Scanner API  
          if [ -d "api" ]; then  
            echo ""  
            echo "🔍 Scan SAST API..."  
              
            API_JS_COUNT=$(find api -type f -name "*.js" ! -path "*/node_modules/*" ! -path "*/coverage/*" | wc -l)  
            echo "  Fichiers à scanner: $API_JS_COUNT .js"  
              
            if [ $API_JS_COUNT -gt 0 ]; then  
              find api/src -type f -name "*.js" ! -path "*/node_modules/*" ! -path "*/coverage/*" > /tmp/api-files.txt  
                
              if [ -s /tmp/api-files.txt ]; then  
                echo "  Fichiers trouvés:"  
                head -5 /tmp/api-files.txt  
                  
                cat /tmp/api-files.txt | xargs eslint \  
                  --config .eslintrc.security.json \  
                  --format json \  
                  --output-file sast-eslint-api.json 2>&1 || echo "  ⚠️ Scan terminé avec avertissements"  
              fi  
                
              echo "  ✅ Scan API terminé"  
            else  
              echo "  ⚠️ Aucun fichier JS trouvé"  
            fi  
          fi  
            
          # Vérification finale  
          echo ""  
          echo "📊 Résultats:"  
          for file in sast-eslint-client.json sast-eslint-api.json; do  
            if [ -f "$file" ]; then  
              SIZE=$(stat -c%s "$file" 2>/dev/null || stat -f%z "$file" 2>/dev/null)  
              ISSUES=$(cat "$file" | jq '[.[].messages[]] | length' 2>/dev/null || echo "0")  
              echo "  ✅ $file: $SIZE bytes, $ISSUES problèmes détectés"  
            else  
              echo "  ❌ $file: MANQUANT"  
              echo '[]' > "$file"  
            fi  
          done  
        continue-on-error: true  
  
      - name: Verify Files Before Upload  
        if: always()  
        run: |  
          echo "🔍 Vérification finale avant upload..."  
          ls -lh sast-eslint-*.json || echo "❌ Aucun fichier trouvé"  
          pwd  
          find . -name "sast-eslint-*.json" -type f  
  
      - name: Upload SAST Reports  
        uses: actions/upload-artifact@v4  
        if: always()  
        with:  
          name: sast-reports  
          path: |  
            sast-eslint-client.json  
            sast-eslint-api.json  
          retention-days: 30  
          if-no-files-found: warn